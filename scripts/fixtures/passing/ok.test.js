// Fixture for selfcheck.test.js: a test file that always passes.
// Used to verify selfcheck.js reports PASS and exits 0.
const assert = require("assert");

assert.strictEqual(1 + 1, 2, "arithmetic works");
